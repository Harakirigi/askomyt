'use client';

import ContactMeSection from "@components/sections/contactMe";

const ContactPage: React.FC = () => {
    return (
        <ContactMeSection backButton />
    );
};

export default ContactPage;