'use client'

import PropTypes from "prop-types"

const Button = (
    className,
    variant,
    color,
    children,


) => {
    return (
        <button className="">
            {children}
        </button>
    )
}

export default Button